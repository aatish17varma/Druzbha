// TODO: File representing an entire pipeline
// TODO: Work on this

//use pipeline_stage::PipelineStage;

struct Pipeline {
 //  pipeline_stages : Vec<PipelineStage>,
}
// TODO: Test this once PipelineStage has been
// defined
impl Pipeline {
    /*
  pub fn new (t_stages : &Vec <PipelineStage>) -> Self {
    Pipeline {stages : t_stages.clone() }
  }

  pub fn tick (&self, packet : Phv) -> Phv {
    assert (self.stages.len() >0);
    if (self.stages.len()==1){
      self.stages[0].tick (packet)
    }
    for i..self.stages.len()-1{
      self.stages[i].tick (packet);
    }
    self.stages[self.stages.len()-1].tick(packet)
  }
  */
}


